# dmi-public
Digital Material Identifier (DMI) resources

The resources are distributed through the `docs` directory via github-pages. 
The resources are generated using `generate_resources.ipynb` and the data contained in `data`.